/**
 * Разделы, которые можно выключить целиком, не удаляя их код и данные.
 *
 * «Темп» (задачи сотрудников) выключен 18.09.2026 по решению владельца: раздел
 * пока не прижился, и что с ним делать дальше, решат позже. Выключен — значит:
 * нет пункта в меню и в справке, страница показывает заглушку, API отвечает 404,
 * а система перестаёт сама заводить задачи по просрочкам и долгам. Таблица
 * задач остаётся в базе нетронутой — включить обратно можно одной строкой.
 */
export const TASKS_ENABLED = false;

/** Ответ API выключенного раздела: 404, а не 403 — прав тут ни у кого нет */
export function featureDisabled(title: string) {
  return Response.json({ error: `Раздел «${title}» временно отключён` }, { status: 404 });
}
